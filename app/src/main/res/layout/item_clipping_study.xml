<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="@dimen/clipping_card_size"
    android:layout_height="@dimen/clipping_card_size"
    android:layout_marginEnd="@dimen/default_half_margin"
    android:layout_marginStart="@dimen/default_small_margin"
    >

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ImageView
            android:id="@+id/clipping_item_thumbnail"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            />

        <TextView
            android:id="@+id/clipping_item_name"
            android:layout_width="match_parent"
            android:layout_height="@dimen/clipping_card_text_background_height"
            android:layout_alignParentBottom="true"
            android:background="@color/colorPrimary"
            android:textColor="@android:color/white"
            android:ellipsize="end"
            />

    </RelativeLayout>

</android.support.v7.widget.CardView>